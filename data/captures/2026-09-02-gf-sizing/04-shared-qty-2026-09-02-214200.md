# Batch C · the shared lever — open-position quantity 1 -> 26 (G-3b)
Executed 2026-09-02 ~21:30–21:42 ET, Claude in Chrome, Automation Library.
Authority: `R-2026-09-01-GF-SIZING` G-3b, N = 26, SIGNED — Andy — 2026-09-02.
Gate satisfied first: `02-scanner-attachments-2026-09-02-210500.md` (8/8 GF, 2/2 hash match).
Canary detached first: `03-canary-detach-2026-09-02-212700.md` — so this edit reaches **7 bots**,
of which `GF-QQQ-IC-Ride-Delta` is AUTOS OFF and does not trade. **6 arms are live at 26 ct.**

## Path used, per scanner (identical both times)
Library -> fresh open of the automation -> action card `Open Symbol Short <Put|Call> Spread` ->
`Position Size` -> Current-Value row -> `input[name="quantity"]` set to 26 -> panel **Save** ->
automation-level **Save** (`a.saveclose`, the real commit, trap 7) -> **HARD RELOAD** ->
re-open fresh -> re-read `a5.bots.acedit.routine`.
No version number was used as evidence. No save toast was used as evidence.

## Result — `amount` at `root.outcome.outcomes.0.outcomes.0.outcomes.0.outcomes.0.outcomes.1.input`
| | before | after (re-read post hard reload) |
|---|---|---|
| GF-ScannerA-PutSpread | `{"text":"1 contract","type":"quantity","quantity":1}` | `{"text":"26 contracts","type":"quantity","quantity":26,"textValues":{"quantity":"26 contracts"}}` |
| GF-ScannerB-CallSpread | `{"text":"1 contract","type":"quantity","quantity":1}` | `{"text":"26 contracts","type":"quantity","quantity":26,"textValues":{"quantity":"26 contracts"}}` |

## Config hash pairs
| automation | pre | post | bytes |
|---|---|---|--:|
| GF-ScannerA-PutSpread | `1e5eb9936a1adf067af65a4841d42e755592f7c179f3c0cad477502dfdbfcdc8` | `2c4a96c5e2e3c8503dfe05263b45502f49de607b3e236263211c7e5e283e60d2` | 5379 -> 5425 |
| GF-ScannerB-CallSpread | `a925d490b8a0d2337566f47307fc52470da129935d3bd83d24389c6dc433dfb5` | `a1a48af1caebc35900ac7652b14bb26df084e492fa87aba2fc783c8068a9d7ba` | 5044 -> 5090 |

**These are the new CONFIG HASH values for the GF entry surface.** The pre values are the ones
stamped in `pre-registration-ledger.md` under `R-2026-08-17-GF-ENTRY-METHOD` (ScannerA v12 /
ScannerB v4); they are now superseded for every arm still on the shared pair.

## "Nothing else in the tree changed" — the diff, done properly
Full leaf-by-leaf flatten of `root` before vs after. **Exactly four leaves differ per scanner, and
all four are the same field expressed four ways:**
```
CHANGED  …outcomes[1].input.amount.quantity      1 -> 26
CHANGED  …outcomes[1].input.amount.text          "1 contract" -> "26 contracts"
ADDED    …outcomes[1].input.amount.textValues.quantity = "26 contracts"
CHANGED  …outcomes[1].input.textValues           (the action's display-string mirror; the only
                                                  substring that differs inside it is
                                                  "Position Size" value "1 contract" -> "26 contracts")
```
`inputs` array: **identical**, both scanners. `name`: identical.
Unchanged and re-read inside the action: Expiration `exactly 0 days` · wing `$2.00 below/above the
short leg` · short strike `-.10 delta` / `.10 delta` · Price `100% of bid/ask` · Exit Options
binding `GF_EXITS_PUT` / `GF_EXITS_CALL` (same input ids) · Tags `put side` / `call side` ·
Position Criteria `Mid price is between $0.08 -` · the whole gate chain (after 1:31pm / before
2:00pm / symbol change % > -0.75 / < 0.75 / "Bot opened a position with <put|call> side today" ->
NO branch).

## Propagation confirmed on an arm, not just in the library
Opened `GF-QQQ-IC-Ride` (`BOTfw5TkkCRF4417860701930934951`) -> its `GF-ScannerA-PutSpread` row ->
the model read from the bot's own page: `amount.quantity === 26`, `text "26 contracts"`.
Ride's own state re-read at the same time and unchanged: ACCOUNT Paper Trading · group IC ·
`status "on"` · `disableExits 0` · `seed 2500` (Batch D changes this) · `posLimit 2` /
`posLimitDay 2` · tags `experiment,gfam,arm ride,pr 14`.

## Risk now expressed by this edit
26 ct x $193 measured risk/contract = **$5,018 per position (larger side of the condor)**, per arm,
per side. Six live arms -> family daily structural maximum ~$30,108 at 6 arms
(the ruling's $35,000 figure assumes 7 arms including Ride-Delta, which is OFF).

## Files
`04a-scannerA-POST26-routine.json` · `04b-scannerB-POST26-routine.json` — post-edit payloads,
each file's own sha256 equal to the config hash above.
