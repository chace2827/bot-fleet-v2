# Batch B · Canary detach to bot-local scanners — G-3d route (a)
Executed 2026-09-02 ~21:05–21:27 ET, Claude in Chrome. ET clock from OA's own page header.
Bot: `GF-QQQ-IC-Canary` `BOTfw5TkkCRF4417860774419022836`.
Authority: `R-2026-09-01-GF-SIZING` G-3d route (a), SIGNED — Andy — 2026-09-02
("detach to bot-local, keep 1ct"). Precedent: `06-ride-delta-scanner-diff-2026-08-31.md`.

## 1. PRE state (bot's own page, before any edit)
ACCOUNT **Paper Trading** · BOT GROUP `IC` · AUTOMATIONS **ON** (`a5.bots.bot.status === "on"`)
· EXIT OPTIONS **ON** (`disableExits === 0`) · ALLOCATION $2,500 (`seed === 2500`)
· DAILY POSITIONS **2 per day** · POSITION LIMIT **2 at once** · DAY TRADING Allowed
· Scan speeds: automations 1m, exit options 1m · Symbols QQQ
· Tags `experiment,gfam,arm canary,pr 20` · Bot Inputs GF_EXITS_PUT / GF_EXITS_CALL, both
  "Profit: 5%, Expiration: 10 minutes".

Automations (3), all `i.sticon title = "Automation is on"`:
```
SCANNERS  GF-ScannerA-PutSpread   (shared library)  with 1 input
          GF-ScannerB-CallSpread  (shared library)  with 1 input
TRIGGERS  GF-Backstop-1552-FlatClose (shared)       Mon-Fri 3:52pm EST
```
⚠️ NOTE, CONTRARY TO THE 08-31 RECORD: `a5.bots.bot.scanning` reads **false** on this bot with the
master switch demonstrably **ON** (header toggle ON, `status:"on"`, `i.sticon` "Automation is on",
roster capture AUTOS=ON). At 21:0x ET the market is closed, so `scanning` is a *runtime* flag, not
the master switch. The field used as AUTOMATIONS evidence on 2026-08-31 (Edit 1) is therefore not a
reliable master-switch surface out of hours. `status` is. Recorded, not acted on.

## 2. Bot-local copies created
Route used: the automation row's `...` menu -> **Duplicate** -> dialog "Duplicate automation?" ->
**"Yes, create a copy"** (the other options were "No, use the same automation" and "Cancel") ->
name dialog -> typed name -> Save -> the row's Edit Settings drawer -> Save.
"Yes, create a copy" produces an **independent, bot-local** object: the Automation Library still
lists exactly its original four automations afterwards (screenshot + DOM read), so nothing was
added to the shared library.

| copy | source | bytes | config hash `sha256(JSON.stringify({name,inputs,root}))` |
|---|---|--:|---|
| `Canary-Scan-Put` | GF-ScannerA-PutSpread | 5373 | `29abf4e4795ac408f9c4a56c31da32b6b795671af41ec100ed03b94214efb04a` |
| `Canary-Scan-Call` | GF-ScannerB-CallSpread | 5038 | `67763502669d1b822a4f3a1a47a5c72bbce8e64ac4d1aa4f0a2875bde749c8d8` |

The hashes differ from the shared originals **because `name` differs** — expected, and stated in
the ruling. The proof of equivalence is the **field diff**, done on the two saved payloads:

```
Canary-Scan-Put  vs GF-ScannerA-PutSpread : inputs equal ignoring node ids -> True
                                            root   equal ignoring node ids -> True
Canary-Scan-Call vs GF-ScannerB-CallSpread: inputs equal ignoring node ids -> True
                                            root   equal ignoring node ids -> True
```
(Comparison recursively drops the keys `id,uid,rid,nid,_id` and compares the rest exactly.
Byte lengths differ by exactly the name-length delta: 5379-5373 = 6 = len("GF-ScannerA-PutSpread")
- len("Canary-Scan-Put"); 5044-5038 = 6 likewise.)

Open-position action, both copies, same path as the originals:
```
root.outcome.outcomes.0.outcomes.0.outcomes.0.outcomes.0.outcomes.1.input
amount = {"text":"1 contract","type":"quantity","quantity":1}
```
**quantity = 1 — Canary stays at 1 contract, which is the whole point of route (a).**

## 3. Shared pair detached from Canary — this bot only
The row `...` menu -> **Delete**. The confirmation dialog read, verbatim and in full:
> **Remove scanner?**   [Cancel] [Yes]

It says nothing about the library and nothing about other bots, so per the dispatch's rule it was
confirmed. Done once for `GF-ScannerA-PutSpread`, then once for `GF-ScannerB-CallSpread`.
⛔ Neither library automation was deleted.

## 4. POST state — after a HARD RELOAD, two surfaces
Automations (3), `i.sticon title` all "Automation is on":
```
SCANNERS  Canary-Scan-Put   (bot-local)  with 1 input
          Canary-Scan-Call  (bot-local)  with 1 input
TRIGGERS  GF-Backstop-1552-FlatClose (shared, retained)
```
Model re-read: `status "on"` · `disableExits 0` · `seed 2500` · `group.name "IC"` ·
`posLimit 2` · `posLimitDay 2` · `account.name "Paper Trading"` ·
`tags "experiment,gfam,arm canary,pr 20"` — **all unchanged from PRE**.

## 5. Library check — the blast-radius control
| automation | attached before | attached after |
|---|--:|--:|
| Defang-Mon-S2-StrikeTouch | 2 | 2 |
| GF-Backstop-1552-FlatClose | 8 | 8 |
| GF-ScannerA-PutSpread | 8 | **7** |
| GF-ScannerB-CallSpread | 8 | **7** |

Membership after, both scanners, identical lists — **the 8 minus Canary**:
`GF-QQQ-IC-PT50, -Touch0, -Ride-Delta, -SL200, -Ride, -Trail, -SL100`.

Config hashes re-read after the detach, each from a fresh open with a hard reload between opens:
```
GF-ScannerA-PutSpread  1e5eb9936a1adf067af65a4841d42e755592f7c179f3c0cad477502dfdbfcdc8  (5379B)
GF-ScannerB-CallSpread a925d490b8a0d2337566f47307fc52470da129935d3bd83d24389c6dc433dfb5  (5044B)
```
`cmp` against the pre-edit payloads: **BYTE-IDENTICAL, both.** The library was not modified and no
other GF arm was affected. No STOP condition fired.

## 6. Incident disclosed
Two `Runtime.evaluate` calls returned the documented ~45s timeout, and once the page stopped
accepting script injection for ~90s. Per the runbook **nothing was re-fired**; state was re-read
after a hard reload each time. The first timeout's work had in fact committed (its file download
landed); the second had not (all five automations still present). One scripted attempt at the
detach was refused by the session's own tool-permission classifier; the detach was then performed
as an ordinary UI click, with the dialog screenshotted and read before confirming.

## Files
`03a-canary-scan-put-routine.json` · `03b-canary-scan-call-routine.json` — the copies' payloads.
