# Sprint plan — 2026-08-04 → 2026-08-07 (Max-window front-load)

*Written 2026-08-04, approved by Andy in session. Purpose: front-load every task whose quality
depends on the strongest available model before the 2026-08-07 plan downgrade (Max → Pro), and
deliberately defer mechanical, checklist-verified work past it. Working budget ≈2–4 h/day.*

**Context verified at planning time:** the OA automation-authority amendment (Claude executes
OA edits directly, two-layer verification, Andy retains revoke) was recorded 2026-08-04 in
CLAUDE.md §5/§7/§9.1a, build-plan.md §5, daily-loop-spec.md §8, oa-ops-runbook.md (header, §4.0,
§6), docs/state.md, and session-log (2026-08-04 entry) — re-verified this session by direct
`device_bash` grep + sha256 (10/10 staged/device hash matches). The migration tracker lags
state.md on ≥3 items (git remote, Chrome-direct decision, mirror_baseline.csv) — explained by
the 2026-08-04 session's banner-only tracker update; refresh queued as Task 11.

---

## 1. Ranked triage

Urgency = quality lost if deferred past Aug 7 onto a weaker model.

| Rank | Item | Source | Why this rank | Model | Est. |
|---|---|---|---|---|---|
| 1 | Seven-decision memo: D-1, D-3, D-4, tournament shared-automation conflict, entry-pricing ban scope, §8.2 attribution guard, clone residue | state.md D-table; tracker t2/p4 | All amend or gate the frozen plan; wrong calls expensive and hard to detect; D-1 + tournament conflict block the greenfield family | Opus + adversarial-verify subagents | 90 min |
| 2 | Research-loop spec deep review (5 §5a defects, unsigned RISK-rung substitution, TIME_* slots, §10 Claude-filled values) | spec §5a/§10; tracker rl | Andy explicitly asked; a signed live-gating spec with a known unsigned amendment inside it | Opus + 3 adversarial reviewers | 60–90 min |
| 3 | Pilot clone completion via Chrome (steps 5c, 6–9, FINISH) + clone loose ends | pilot card; state.md | First full Claude-driven 9-step ritual — first-time OA automation writing state; validates ritual so the 3 remaining clones go mid-tier. ~70–90 clicks into Chrome | Opus | 90 min |
| 4 | Day-0 runbook adversarial audit (D-3 gate, maxexits, two-window Bot Schedule, DST, §9 #5, expiration protocol, paper notifications) | state.md; platform-ref §13 | Day-0 executes ~mid-Aug on a weaker model; front-load all Day-0 judgment now; deferral cost unrecoverable | Opus + 4 lens subagents | 90 min |
| 5 | Greenfield IC family + hedge-arm design spec | build-plan §2D; tracker p4 | Largest single judgment deliverable; consumes D-1 + tournament rulings; builds mechanical later, design is not | Opus | 90–120 min |
| 6 | R-01…R-07 authorization package + gated edits (runbook §5 Trap 1, card Step 2, build-plan §2B wording) | state.md; tracker gt | Three docs still assert falsified claims; contested-rewrite judgment is Opus-tier | Opus review; edits any tier | 45–60 min |
| 7 | Track B loss-side arm design + hard pre-registration drafts | spec §4; tracker rl/p4 | Hypothesis/kill-criterion writing is judgment; depends on Rank 2 rulings; loss side first per mirror baseline | Opus | 60–90 min |
| 8 | Mirror funding memo (10 mirrors, n=174 positions; four positive-median/negative-mean) | state.md; mirror_baseline.csv | Funding decided at Day-0 post-downgrade; analysis quality is model-bound | Opus | 30–45 min |
| 9 | Decision-propagation edits (D-2 → 5 ICs/day; D-4 retirement per ruling) | state.md; tracker t2 | Mechanical once authorized; clear success test | Sonnet | 30 min |
| 10 | Pre-registration drafts, untouched nine | tracker p4 | Template-driven; hard entries live in Rank 7 | Sonnet | 45 min |
| 11 | Tracker refresh + [ANDY]→[CLAUDE] retag | tracker banner | Mechanical with checklist from this triage; Andy's visual confirmation closes it | Sonnet | 20–30 min |
| 12 | research_loop.py freeze + wire as stage 9 | tracker rl | Gated on Rank 2 rulings; mechanical vs existing pattern; 23/23 checks green — safe post-downgrade | Sonnet — deferred | 45 min |
| 13 | Archive sweep (20), deletions (2), 3 remaining clones | build-plan §2; tracker p4 | Checklist + validated ritual + two-layer verification; account inactive — safe post-downgrade | Sonnet — deferred | 3–4 h |
| 14 | Cleanup Blocks 3–4, Baseline forensic, evidence-standards redesign, Notion, liveness bot-log side | tracker p6/p7 | Deferred by prior decisions, gate nothing, or need Day-0 data | any — deferred | — |

Not queued by standing rule: `bots_config_v2.csv` (BLOCKED — written per-bot as bots are built),
§9 #5 and the DST question (Day-0 observations), Blocks 3–4 (deferred until after pilot/Day-0).

## 2. Day-by-day schedule

### Aug 4 evening (~2.5–3 h)
- Rank 1 seven-decision memo — drafted with 2–3 adversarial subagents attacking each
  recommendation before it reaches Andy (~90 min). Andy rules tonight or Aug 5 morning.
- Rank 2 research-loop review launched in parallel (3 adversarial reviewers), ruling sheet
  same evening (~60 min wall-clock, overlapped).
- Close-out: session log, commit handoff (~15 min).

### Aug 5 (~3–4 h) — depends on Rank-1/2 rulings
- Andy's rulings on both memos (~30 min of Andy's time).
- Rank 3 pilot completion + residue cleanup via Chrome, two-layer verified (~90 min).
- Rank 6 R-01…R-07 package → authorization → gated edits, hash-verified (~60 min).
- Rank 9 decision-propagation edits (~30 min, Sonnet).

### Aug 6 (~3–4 h) — Rank 5 depends on D-1 + tournament rulings
- Rank 5 greenfield IC family + hedge-arm design spec (~2 h).
- Rank 7 Track B arm design + hard pre-registration drafts (~60–90 min).

### Aug 7 (~2–4 h) — last Max day; Rank 4 takes priority if time compresses
- Rank 4 Day-0 runbook adversarial audit (~90 min).
- Rank 8 mirror funding memo (~30–45 min).
- Rank 11 tracker refresh + sprint close-out, commit handoff (~30 min).
- If capacity remains: Task 12 Baseline forensic via parallel subagents (gates nothing).

### Deferred past Aug 7 — and why that is safe
- Archive sweep, 2 deletions, 3 remaining clones: checklist + ritual validated by Rank 3 +
  two-layer verification; account inactive, nothing trades meanwhile.
- research_loop.py freeze/wire: mechanical once rulings land; 23/23 checks green.
- Untouched-nine pre-registrations: template-driven, verifiable (may also land Aug 5–7 if
  time allows — Task 10).
- Cleanup Blocks 3–4: already deferred by the 2026-08-03 decision.
- Day-0 observations (§9 #5, DST, toggle causal check): impossible before reactivation; the
  Rank-4 audit is their insurance.
- Baseline forensic, evidence-standards redesign, Notion: optional research.

**Dependencies:** Rank 1 → Ranks 3 (residue), 5, 9 · Rank 2 → Ranks 7, 12 · Rank 3 → Rank 13 ·
Rank 6 needs Andy's authorization mid-chain.

---

## 3. Task prompts

Each prompt is self-contained and copy-pasteable. Model named per task.

### Task 1 — Seven-decision memo · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted via the device
bridge at mnt/bot-fleet-v2). Today is 2026-08-04. Produce a decision memo covering seven open
decisions so Andy can rule on each in one line.

READ FIRST (fresh, never from memory): CLAUDE.md (§3 source-of-truth, §4 evidence law, §5
discipline rules), docs/state.md (the D-1…D-4 table at top plus the TIER 2 and WRITES MADE TO
THE CLONE blocks), docs/build-plan.md (frozen — note its amendment convention in §5),
docs/oa-platform-reference.md §9, §13, §8.2, docs/oa-ops-runbook.md §3–§5,
docs/research-loop-spec.md §4.

PRODUCE docs/decision-memo-2026-08-04.md with one section per decision. For each: the forcing
fact (cited to file+section), 2–4 options, a recommendation with one-paragraph rationale, the
exact ready-to-paste amendment text for any doc that changes, and a one-line ruling slot for
Andy. The seven:
1. D-1 — "PT% as a Bot Input" is not expressible (only the whole Exit-Options bundle links as
   an input). What replaces it in build-plan.md §5.2/§8.1: Exit-Options-SET as a Bot Input,
   per-arm hand-set values, or drop the input idea. This amends a frozen plan — draft the
   amendment text but mark it EXECUTES ONLY ON ANDY'S "amend the plan".
2. D-3 — ITM Position Action: itmpaper=itmlive=auto sends NO closing order; market is the only
   closing option and fires 15:50, racing the clone's Expiration exit. On paper the stake is
   data quality (synthetic P/L on the loss tail). Recommend a setting for paper now and a
   Day-0 stance for live.
3. D-4 — Retire the Excessive Errors Failsafe as the June-lapse hypothesis (zero June errors
   on both Fortress bots, newest error Apr 16 2026 3:55PM). List every doc/pre-registration
   entry carrying it, the replacement wording, and a ranked shortlist of remaining candidate
   causes for the 2026-06-12 lapse with what evidence at Day-0 would discriminate them.
4. Tournament shared-automation conflict: oa-ops-runbook §3 says fork via Copy (arms NOT
   shared); build-plan §2D + hedge-research §5.2 require a shared automation with one
   differing input. Tier-2 found presets are account-scoped and inputs link at bundle level.
   Resolve the architecture; state which doc gets amended.
5. Entry pricing: platform-reference §7 bans Market pricing on exits only; the Fortress
   enters at Market both sides; the cited failure mechanism ($5.05/contract outside the
   spread, R −1.63) is order-type-specific, not side-specific. Should the ban cover entries?
6. §8.2 attribution guard: the 15:52 backstop and the 15:50 Expiration exit are both Market
   pricing, so only the memo string and the 2-minute gap distinguish them in the fill record.
   Keep as-is, or change one mechanic's pricing to restore attribution?
7. Clone residue: preset TIER2-CHECK4-PUTSIDE now exists (account previously had zero);
   Fortress-ScannerA-PutSpread-CLONE was saved with a re-serialized exits blob (numeric
   payload byte-identical) and still carries its -CLONE name, Bot Group None, Tags empty.
   Recommend keep/delete/revert for each.

VERIFY: every quoted phrase byte-exact against the device file (grep the mounted path, assert
exactly one match). Every aggregate carries its n. After writing, verify the memo by direct
device read + sha256 via a shell command against the mounted path — never trust the write
tool's success message and never use a staged read-back as verification.

DO NOT: edit build-plan.md, oa-platform-reference.md §8, or any frozen/gated doc — the memo
contains draft amendment text only. Do not run git. Do not touch OA. Do not infer anything
from a screen you did not open. Before reporting done, spawn 2 subagents to adversarially
attack your D-1 and tournament-conflict recommendations (prompt them to refute); record
surviving objections in the memo. End by listing changed files for Andy's commit.
```

### Task 2 — Research-loop spec deep review · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted via the device
bridge at mnt/bot-fleet-v2). Today is 2026-08-04. Andy explicitly asked for a deeper review of
docs/research-loop-spec.md before scripts/research_loop.py is wired into daily.sh. The spec
was signed fast ("agreed with all") and building against it surfaced five defects (§5a).
Produce a ruling sheet that makes Andy's review a 10-minute read.

READ FIRST: docs/research-loop-spec.md in full (especially §3, §5, §5a, §10),
docs/oa-export-schema.md, scripts/research_loop.py, docs/hedge-research.md §9, CLAUDE.md §4
(evidence law), docs/state.md ("Research loop" block).

RUN 3 PARALLEL ADVERSARIAL REVIEWERS (subagents), each prompted to refute, not summarize:
(a) statistics — is the §10 gate (both mean and median R, ≥0.10R margin, 95% CI excluding
zero after Bonferroni for 12 variants) coherent and actually computable from what
research_loop.py records? Is Bonferroni across 12 variants × ~20 bots the right correction, or
is the real comparison count 240/night? (b) design — are the 12 variants the right 12? Attack
the unsigned 0.50×/0.75× RISK substitution for the fixed-$ rungs and the two structurally
UNDECIDABLE TIME_* slots (keep as reminders vs replace vs drop to 10). (c) code-vs-spec — does
research_loop.py implement the signed text where it can and flag where it can't? Check the
CONTROL self-test, the n-printing rule, and the verbatim-real-row fixture against
oa-export-schema.md's sign conventions.

PRODUCE docs/research-loop-review-2026-08-04.md: each §5a defect with a recommended ruling and
exact replacement text for the spec; each reviewer's surviving objections (discard refuted
ones, say why); a ruling slot per open item — minimum: (1) sign or reject the RISK-basis
rungs, (2) TIME_* slot disposition, (3) confirm or correct the §10 margin and start condition
Claude filled in. Note explicitly that the spec's §3 set changes only by signature.

VERIFY: quotes byte-exact against the device file (single-match assert). Every statistic
carries its n. Verify the written file by direct device read + sha256 via shell, not the write
tool's response and not a staged read-back.

DO NOT: edit research-loop-spec.md (rulings are Andy's; §5a records defects already), wire
anything into daily.sh, freeze research_loop.py, or run git. End by listing changed files for
Andy's commit.
```

### Task 3 — Pilot clone completion via Chrome · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2) with FULL BUILD AUTHORITY in the Option Alpha web app via Claude in Chrome
(authorized 2026-08-04, CLAUDE.md §5). Complete the pilot clone ritual on
QQQ-IC-0DTE-Fortress Clone (bot_id BOTfw5TkkCRF2717857919585029021) — steps 5c, 6, 7, 8, 9
and FINISH of docs/pilot-clone-card-qqq-fortress.md — and close the clone's three loose ends.
Drive every click yourself; do not queue instructions for Andy.

READ FIRST: docs/pilot-clone-card-qqq-fortress.md in full, docs/oa-ops-runbook.md §1 and §4
(capture ritual + two-layer verification), docs/state.md ("Pilot clone" and "WRITES MADE TO
THE CLONE" blocks — parts 1–2 are done, both Decision Points answered), CLAUDE.md §5, §9.1a.
⚠️ The card's Step 2 (fork automations) is VOID — Trap 1 was falsified 2026-08-03; clones do
NOT share automations by reference. Do not re-run it. ⚠️ The card's date placeholders are
stamped 2026-08-03; captures from this session go under data/captures/2026-08-03-pilot/ with
today's date in filenames — do not restamp the -ARCHIVED- suffix, which is a lineage record.

DO, in order:
1. Loose ends: rename Fortress-ScannerA-PutSpread-CLONE back to its production name, set Bot
   Group to Monitor, set Tags to experiment — per Andy's Task-1 ruling on the residue; skip
   any item his ruling says to leave.
2. Step 5c: confirm no exit uses Market pricing except the 15:50 Expiration exit and the
   15:52 backstop (the flat-close carve-out). Read values, change nothing unless off-spec.
3. Step 6: full capture of every automation tree (expand every caret — collapsed nodes may
   not be in the DOM), Exit Options via print-to-PDF, into data/captures/.
4. Step 7: save Template V1 with the PR-03 pre-registration entry pasted VERBATIM into Notes
   (even where it names a mechanism the build lacks — the card explains why), tag PR-03.
   Then re-open the clone and report whether saving the template disturbed it (open UI check).
5. Step 8: rename original → QQQ-IC-0DTE-Fortress-ARCHIVED-2026-08-03, confirm, rename clone
   to QQQ-IC-0DTE-Fortress, archive the original. Append the rename_map.csv row immediately.
6. Step 9: screenshot both toggles; leave both OFF — turning ON is Day-0 only.
7. FINISH: capture-diff Step 6 vs the Step 0 baseline; any difference outside the exit block
   is a finding, reported not fixed.

VERIFY every edit with both layers (ops-runbook §4): hard reload, then re-read the underlying
input.value / input.checked / data-value — NEVER innerText (three wrong findings), never a
save banner, never a "selected" CSS class. Use element refs for clicks, never coordinates
(1.675× viewport mismatch). Log each self-check match/mismatch. The behavioral (Trades-list)
layer is impossible while the account is inactive — record it as DEFERRED TO DAY-0, not done.
Verify every file written by direct device_bash sha256 against the mounted path.

DO NOT: touch payment, subscription, or authentication surfaces — hard exclusion. Do not
touch QQQ-IC-0DTE-Fortress-NoPT50, the champion, or its -130PM clone. Do not test-fire.
Do not run git. If anything surprises you, stop and describe it — do not improvise a spec.
This run replaces ~70–90 manual clicks of Andy's; note any step where the ritual itself broke
— that is the pilot's real product. End: append docs/session-log.md, update docs/state.md,
list changed files for commit.
```

### Task 4 — R-01…R-07 authorization package + gated edits · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Today is 2026-08-05. Prepare the authorization package for the R-01…R-07
doc edits, get Andy's per-item authorization, then apply exactly what he authorizes.

READ FIRST: docs/oa-platform-reference.md §0.2 — the editing policy that governs this file:
evidence-backed appends need no authorization, inferences from absence are FORBIDDEN, rewrites
of contested sections need Andy's authorization, §8 stays gated. Then:
docs/oa-reconciliation-report.md (R-01…R-20), docs/oa-platform-reference-v3-DRAFT.md,
docs/state.md ("Still needing authorization" block), docs/oa-ops-runbook.md §5 Trap 1,
docs/pilot-clone-card-qqq-fortress.md Step 2, docs/build-plan.md §2B.

PRODUCE docs/r-edit-authorization-2026-08-05.md: one row per proposed edit — R-01…R-07 plus
the three standing items (ops-runbook §5 Trap 1 false shared-automations claim; pilot card
Step 2 void fork step; build-plan §2B "restored exits" wording) — each with: the exact current
text (byte-verified, single-match assert against the device file), the exact replacement, the
evidence citation (fact IDs from data/oa_facts.csv or the dated first-hand observation), and
whether §0.2 classes it as free append vs gated rewrite. Also present the alternative: adopt
oa-platform-reference-v3-DRAFT.md wholesale as the new reference. STOP and get Andy's ruling.

AFTER AUTHORIZATION ONLY: apply each authorized edit as a dated amendment (matching the
convention already used — supersedes-notes, CONTESTED banners left standing where §0.2
requires). build-plan.md §2B changes ONLY on an explicit "amend the plan". Verify each edited
file by direct device_bash sha256 + grep of the new text (single match) — never the write
tool's response, never a staged read-back.

DO NOT: edit anything before Andy rules. Do not touch §8 of the platform reference. Do not
append any inference from absence. Do not run git. End: append docs/session-log.md, update
docs/state.md, list changed files for commit.
```

### Task 5 — Decision-propagation edits · Claude Sonnet (mid-tier)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Today is 2026-08-05. Andy has ruled on the 2026-08-04 decision memo
(docs/decision-memo-2026-08-04.md — read his ruling lines first; apply ONLY what is ruled).
Propagate two settled decisions through the docs:

1. D-2 (decided 2026-08-04): the re-entry ceiling is 5 ICs/day per bot, one bot — position
   limits are 1–10 pickers and an IC is two positions. Find every doc stating or assuming a
   daily limit above 10 or "ten IC re-entries = 20/day" (grep the docs/ tree for candidates;
   likely docs/hedge-research.md and any re-entry spec) and add a dated correction note per
   file's own convention. Do not silently rewrite.
2. D-4 (as ruled in the memo): the Excessive Errors Failsafe is retired as the June-lapse
   hypothesis. Update docs/pre-registration-ledger.md entries that carry it as candidate
   cause, using the memo's replacement wording. oa-platform-reference.md §4.5 gets a dated
   append per its §0.2 policy (evidence: zero June errors, newest error Apr 16 2026 3:55PM,
   error days Apr 16 n=91 and Mar 16 n=138+ on Fortress, Apr 16 n=91 on -NoPT50) — the
   original text stands, banner added.

RULES: quote current text byte-exact and assert a single grep match on the device file before
each edit (anchored insertion, never retyping). Every count carries its n. build-plan.md and
oa-platform-reference.md §8 are untouchable. Verify every edited file by direct device_bash
sha256 + grep of the inserted text — never the write tool's response, never a staged
read-back. Do not run git.

END: append docs/session-log.md, update docs/state.md if a stated fact changed, list changed
files for Andy's commit.
```

### Task 6 — Greenfield IC family + hedge-arm design spec · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Today is 2026-08-06. Write the build spec for the greenfield IC family
(4–6 matched bots) and the rebuilt hedge tournament arms — the design document Phase 4's
fresh builds will be built from, mechanically, after 2026-08-07.

READ FIRST: docs/decision-memo-2026-08-04.md WITH ANDY'S RULINGS — D-1's replacement for
"PT% as a Bot Input" and the tournament shared-automation resolution are inputs to this spec;
if either is unruled, STOP and say so. Then: docs/build-plan.md §2D, §5 (frozen — this spec
implements it, it does not amend it), docs/oa-platform-reference.md §3, §5.2, §6.1a, §8, §9
(all struck rows carry answers), §11, §13, docs/hedge-research.md, docs/oa-ops-runbook.md §2
(template versioning), §3 (groups), docs/pre-registration-ledger.md §2 (entry template),
docs/research-loop-spec.md §4 (Track B context).

PRODUCE docs/greenfield-family-spec.md:
- Per bot: name, underlying, entry logic, exit stack expressed ONLY in controls confirmed to
  exist (§6.1a field roster; Touch $0 challenged side per §6.2; 15:52 Repeating backstop as
  built on the pilot), allocation, position limits (remember: pickers cap at 10, an IC is 2
  positions, ceiling 5 ICs/day), Bot Group, Tags, template-V1 plan with pre-registration in
  Notes per ops-runbook §2.
- Arm-matching mechanism per the tournament ruling, and the PROOF procedure: the capture-diff
  that must show exactly one differing input per arm pair, plus the nightly arm-distinctness
  assert (ops-runbook §3).
- The 15:50/15:52 Market-pricing attribution stance per the memo's §8.2 ruling.
- ITM Position Action assumption per the D-3 ruling, stated explicitly.
- Draft pre-registration entries (hypothesis, kill criterion, sample target, review date)
  for each family bot, marked DRAFT — unsigned.
- A build-order checklist a mid-tier session can execute per-bot with zero design decisions
  left open, each step ending in a two-layer verification (ops-runbook §4).

CONSTRAINTS: nothing in §11's not-expressible list may appear in any mechanic — name the
primitive each mechanic is built from (the HedgeD lesson: undocumented substitution at a
platform limit, −$15,376). No mechanic may depend on the Exit Options panel as evidence.
Sizing per build-plan §5: experiments 1 lot, arms identical allocation. Spawn 2 adversarial
subagents to attack the finished spec — one for platform-expressibility, one for
confound/matching defects — and record surviving objections in the spec.

VERIFY the written file by direct device_bash sha256. DO NOT: edit build-plan.md, touch OA,
or run git. End: append docs/session-log.md, update docs/state.md, tracker note, list changed
files for commit.
```

### Task 7 — Track B loss-side arms + hard pre-registrations · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Today is 2026-08-06. Design the first Track B arms — real paper bots, not
counterfactuals — per docs/research-loop-spec.md §4: LOSS SIDE first, ≤8 slots total.

READ FIRST: docs/research-loop-review-2026-08-04.md WITH ANDY'S RULINGS (the RISK-basis rung
ruling and TIME_* disposition change what arms are worth slots; if unruled, STOP). Then:
docs/research-loop-spec.md (§3, §4, §5, §10), docs/state.md ("Research loop" and "Mirror
baseline" blocks), docs/oa-platform-reference.md §6.1a (Stop Loss $ = dstop and Touch both
confirmed to exist 2026-08-04), §11, docs/pre-registration-ledger.md §2,
data/mirror_baseline.csv (n=174 positions, 10 mirrors — the loss-tail rationale),
docs/greenfield-family-spec.md if it exists (shared conventions).

PRODUCE docs/track-b-arms-spec.md: 2–4 first arms, each: the question it answers that Track A
cannot (fills vs marks — Track A's error is one-sided toward tighter targets), the exact
control bot it pairs with, the ONE differing input, full bot config in confirmed controls
only, slot budget accounting (≤8 total, Pro cap 50 bots vs ~20 used — state your n for
"used" from the disposition table), and a complete DRAFT pre-registration entry per arm:
hypothesis, kill criterion, sample target consistent with the §5 graduation gate (n≥100, 6
months, regime change, pre-declared margin), review date, config-capture-hash placeholder.
Mark every entry DRAFT — signing is Andy's, at Day-0.

RULES: no arm may test a variant outside the signed §3 set unless Andy's review ruling added
it. Time-exit questions REQUIRE an arm (structurally undecidable from MFE/MAE) — say whether
one of the first arms should be a time arm, given the rulings. Every aggregate carries its n.
Verify the written file by direct device_bash sha256. DO NOT: build anything in OA yet, edit
the spec, wire research_loop.py, or run git. End: append docs/session-log.md, list changed
files for commit.
```

### Task 8 — Day-0 runbook adversarial audit · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Today is 2026-08-07 — the last day of strong-model capacity. Day-0
(reactivation, ~mid-Aug) will execute on a weaker model, so every judgment call must be
front-loaded into the runbook NOW. Adversarially audit docs/reactivation-runbook.md against
everything learned since it was written.

READ FIRST: docs/reactivation-runbook.md in full, docs/state.md (D-table, TIER 1/TIER 2/
PHASE 6 blocks), docs/oa-platform-reference.md §4.1's RESOLVED block, §9, §10, §13 (ITM
action, maxexits, Bot Schedule two-windows, paper notifications), docs/oa-ops-runbook.md §4,
docs/build-plan.md §2–§3, docs/daily-loop-spec.md, docs/decision-memo-2026-08-04.md rulings.

RUN 4 PARALLEL LENS SUBAGENTS, each prompted to find what the runbook MISSES:
(a) new platform facts — is every 2026-08-04 finding reflected? Checklist to verify against:
D-3 ITM action must be SET and read back before capital is live; maxexits read (currently 0 =
Unlimited — a single account-wide switch that can cap every bot's closes); Bot Schedule is
TWO independent windows (Automations→15:55, Exit Options→15:59) not one; the Repeating
carve-out (the 15:52 backstop is NOT bound by Bot Schedule); paper notifications unchecked =
no open/close emails while the fleet is paper; expiration-protocol read.
(b) deferred observations — §9 #5 (re-apply Exit Options side-effect-free?), the DST/"Market
Time (EST)" question (ntime=1552 vs a startDate serializing as 16:52 EDT — needs a Day-0
observation), the EXIT OPTIONS toggle CAUSAL check (§10: if toggles read ON and Trades lists
show no PT rows, the lapse mechanism is REFUTED and §4.5 returns to contention). Does the
runbook sequence each one, with a decision tree for each outcome?
(c) sequencing — LEDGER_START set first; ride-or-close on the 5 open mirror positions
(QQQ long call ×4, Tasty Condor ×1) decided before anything trades; re-arm sweep covers the
9 leave-in-place bots ONLY; order-level verification (Trades list, inverted for the two
controls) gates entries. Any step that could run against an unset precondition?
(d) failure modes — for each Day-0 check: what does the runbook say to DO when the check
FAILS? A check with no failure branch is a finding.

PRODUCE docs/day0-audit-2026-08-07.md: findings ranked by severity, each with the exact
runbook amendment text. Apply the amendments the runbook's own conventions allow (it is not
frozen); anything touching build-plan.md is draft text only, gated on "amend the plan".
Discard subagent findings you can refute; say why.

VERIFY: quotes byte-exact, single-match assert against device files. Verify written/edited
files by direct device_bash sha256. Absence is not observation — a control not seen means a
screen not opened; never write an inference from absence into any doc. DO NOT: touch OA, run
git. End: append docs/session-log.md, update docs/state.md, list changed files for commit.
```

### Task 9 — Mirror funding memo · Claude Opus (strongest available)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Today is 2026-08-07. Write the funding analysis for the live mirrors so
the Day-0 funding decision (made post-downgrade) is a read, not an analysis.

READ FIRST: data/mirror_baseline.csv and its receipt in data/receipts/ (the baseline is an
ANCHOR — never recompute it), scripts/build_mirror_baseline.py (methodology),
docs/build-plan.md §3 (the mirror-funding exception — the ONLY place pre-cutover numbers may
enter a decision), §2C (the 7 live mirrors), docs/oa-mirror-reference.md §3 (evidence
standards), CLAUDE.md §4 (compare by R, never raw P/L; every aggregate carries its n),
docs/oa-export-schema.md (ror is the R basis, not returnPct).

PRODUCE docs/mirror-funding-memo-2026-08-07.md: per mirror — n, mean R, median R, win rate,
max drawdown in R, and a fund / watch / kill recommendation with rationale. Address head-on
the known finding that four mirrors have positive median R and negative mean R (win often,
lose money — the loss tail pays for the wins): state per mirror whether the tail is thin-n
noise or structural, and what additional n would change the verdict. Apply the evidence law:
anything below T2 with n≥100 / 6 months / a regime change cannot support a live-capital
decision — say explicitly which mirrors clear the bar for a FUNDING decision versus a
KEEP-WATCHING one. Note the 5 still-open mirror positions (QQQ long call ×4, Tasty Condor ×1)
resolve into the baseline layer under the straddle rule and are Andy's Day-0 ride-or-close
call — reference, don't decide.

RULES: every number from the CSV read this session, none from memory; every aggregate carries
its n; R on the ror basis. Recommendations are Andy's to accept — mark them DRAFT. Verify the
written file by direct device_bash sha256. DO NOT: recompute or overwrite mirror_baseline.csv
(the script refuses without --force; do not use --force), touch OA, run git. End: append
docs/session-log.md, list changed files for commit.
```

### Task 10 — Pre-registration drafts, untouched nine · Claude Sonnet (mid-tier)

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Draft pre-registration entries for the nine leave-in-place bots so Day-0
is signing, not authoring.

READ FIRST: docs/pre-registration-ledger.md (§2 template, existing drafted entries — extend,
do not duplicate), docs/build-plan.md §2C (the nine: DIR-SPX-PutVIX22-SL75,
DIR-SPX-CallVIXdrop, and the 7 mirrors: 3DTE $140-$350, Nigiri-Paper-v1, QQQ long call,
Friday 14 DTE Broken Wing IB (B-70), Trendy-Paper-v1, 60min-ORB-10W-Paper-v1, Tasty Condor),
§5 (pre-registration fields), data/bots_meta.csv (pillar/role per bot — read fresh),
docs/decision-memo-2026-08-04.md (D-4 ruling: do not name the Excessive Errors Failsafe as
the June-lapse cause in any new entry).

PRODUCE: one DRAFT entry per bot in docs/pre-registration-ledger.md — hypothesis, kill
criterion, sample target consistent with CLAUDE.md §4's gates, review date, and
config-capture hash marked PENDING DAY-0 CAPTURE (the capture does not exist yet; do not
invent one). Mirrors are watch-only: their entries state the funding question and point at
the mirror baseline, not a strategy hypothesis. PutVIX22's entry must reflect that 0
positions in 22 days is its gate working, not a defect.

RULES: append per the ledger's own format; anchored insertion with a uniqueness assert on
the anchor; entries are DRAFT — unsigned, signing is Andy's Day-0 act. Verify the edited
file by direct device_bash sha256 + grep for each new entry header (single match each).
DO NOT: sign anything, edit existing entries except as the D-4 ruling directs, touch OA, run
git. End: append docs/session-log.md, list changed files for commit.
```

### Task 11 — Tracker refresh + retag · Claude Sonnet (mid-tier)

```
You are working in the bot-fleet-v2 project. Refresh the bot-fleet-migration tracker artifact
(update_artifact) to current truth and retag stale [ANDY] items.

READ FIRST (fresh): docs/state.md in full, docs/session-log.md entries dated 2026-08-04
onward, CLAUDE.md §5 and §9.1a. Stage the artifact via device_stage_files
artifact_ids:["bot-fleet-migration"] to get current content — but treat the stage-back as
UNRELIABLE (three confirmed stale serves); reconcile against state.md and prefer state.md
wherever they conflict.

KNOWN STALE ITEMS to fix (verify each against state.md before changing): the "NO GIT REMOTE"
item — resolved 2026-08-04, private repo chace2827/bot-fleet-v2, first push c290429; the
"TRIAL: Chrome-direct" item — decided 2026-08-04, docs amended; mirror_baseline.csv — WRITTEN
2026-08-04, do not recompute; the git-init item in Phase 1 — done. Add items/status for: the
2026-08-04 sprint plan (docs/sprint-2026-08-04.md), the decision memo and its rulings, the
research-loop review, pilot completion status per session-log.

RETAG per the banner's instruction: [ANDY]→[CLAUDE] ONLY for items that are OA edits under
the amended authority (archive sweeps, deletions, clone builds, toggle screenshots, captures);
KEEP [ANDY] for genuine decisions (plan review, signing variant sets, authorizations,
ride-or-close, reactivation/payment). When unsure, keep [ANDY] — miscategorizing a decision
as an edit is the failure mode the banner warns about.

VERIFY: update_artifact's success response is a claim, not evidence (CLAUDE.md §9.1a) —
and the stage-back cannot verify it either. State plainly in your close-out that the tracker
update requires ANDY'S VISUAL CONFIRMATION, and ask for it. DO NOT run git. End: append
docs/session-log.md, list changed files for commit.
```

### Task 12 (optional, capacity burn) — Baseline forensic · Claude Opus + parallel subagents

```
You are working in the bot-fleet-v2 project (repo at ~/bot-fleet-v2, mounted at
mnt/bot-fleet-v2). Optional research — gates nothing (build-plan.md §6). Forensic on
QQQ-IC-0DTE-Baseline: −$31,580, 38% of the v1 fleet loss, never audited. The bot is archived
and the data frozen; this is for the lesson, not for any live decision.

READ FIRST: docs/history-index.md (use the index, do not browse the archive),
data/archive/README-v1-ledger.md, data/archive/trades.csv (frozen — read-only input for
HISTORY, never a reporting input), data/captures/oa_export_positions_2026-07-30.csv,
docs/oa-export-schema.md (premium is SIGNED — use openPrice; ror is the R basis), CLAUDE.md
§4 (units: per condor; every aggregate with its n; any condor Exp(R) computed before the
2026-07-31 denominator fix is flattered — recompute, never quote).

FAN OUT 3 SUBAGENTS: (a) time structure — daily/weekly P/L in R, drawdown anatomy, was the
loss concentrated or chronic, and does it straddle the 2026-06-12 exit-engine death?
(b) trade anatomy — loss-tail decomposition, MFE/MAE patterns on the worst n positions
(state n), expired-vs-closed mix, evidence of exits firing or not; (c) config archaeology —
what the frozen record and history index say the bot was, with the caveat that
data/archive/bots_config.csv is hand-written and proven wrong on 3 of 4 audited bots (cite
it only as "the record claimed", never as fact).

PRODUCE docs/baseline-forensic-2026-08-07.md: what killed the bot, ranked hypotheses with
the evidence tier of each, the transferable lesson for the v2 fleet if any, and an explicit
"gates nothing" banner. Every aggregate carries its n; compare by R never raw P/L; label
units per condor/per leg every time.

VERIFY the file by direct device_bash sha256. DO NOT: write to any CSV, touch the archive
folder, touch OA, run git, or promote any conclusion into a v2 doc — this is a standalone
report. End: append docs/session-log.md, list changed files for commit.
```
